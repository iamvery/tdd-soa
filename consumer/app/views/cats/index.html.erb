<h1>Cats</h1>
<%= link_to 'Add', new_cat_path %>
<ul>
  <% @cats.each do |cat| %>
    <li>
      <%= cat.name %> (<%= cat.age %>)
      <%= link_to 'Remove', cat_path(cat.id), method: :delete, confirm: 'Sure?' %>
    </li>
  <% end %>
</ul>
